# 上下文

## 数字资产

- **数字资产**：已登录用户私有、由服务端持久化并按用户隔离的可管理资源；与开发者维护、用户只读的图片提示词库相互独立。
- **文本资产**：数字资产首期支持的唯一类型，用于保存可复用提示词的标题、完整内容、收藏状态和个人标签。
- **个人标签**：用户私有且可复用的数字资产分类；名称去除首尾空格并按大小写不敏感规则判重。无资产绑定时继续保留。

## 绘图工作台

- **绘图工作台**：已登录用户在站内以用户会话调用文生图模型的个人页面；不提供公开作品、用户投稿或社交互动。
- **绘图历史**：绘图工作台中仅对生成者可见、由当前浏览器本地留存的生成结果及其可复用请求参数；不设业务数量上限。浏览器拒绝写入时，删除最早图片后重试；仍失败则只展示本次结果并提示其未写入历史。生成请求固定要求 `b64_json`，前端转换为 `Blob` 保存。
- **图片模型**：当前用户在所选分组中可调用、且服务端声明支持 `image-generation` 端点的模型。
- **图片提示词库**：开发者维护、用户只读的 GPT Image 2 图片提示词案例集合；内容以 `freestylefly/awesome-gpt-image-2` 为唯一权威来源，按需人工更新快照，不承诺与上游实时同步。选择案例会将其原文提示词填入绘图工作台的输入框。
- **精选词库**：管理员在绘图工作台内维护、所有绘图工作台用户只读的推荐提示词集合；与图片提示词库和用户私有的数字资产相互独立。精选提示词包含标题、描述、原文提示词和对象存储封面，选择后只填入绘图输入框，不自动生成图片。
- **提示词润色**：用户在绘图工作台显式触发的两阶段文本模型调用；固定使用 `gpt-5.6-terra`，先依据上游 GPT Image 2 Skill 快照选择唯一模板，再结合原始提示词、画面比例和参考图生成最终提示词。它不是图片生成 Relay 的隐式预处理。
- **提示词案例**：图片提示词库中的单个可复用案例；保留上游标题、原文提示词、来源与分类信息，不对收录内容做额外筛除。
- **提示词类别**：提示词案例按主要用途划分的单一类别。
- **提示词风格**：提示词案例可同时具有的一个或多个视觉表现标签。
- **提示词场景**：提示词案例可同时具有的一个或多个使用情境标签。
- **图片提示词库封面图**：图片提示词库案例的可选视觉缩略图，通过 GitHub 加速代理加载上游图片，不在本站保存图片快照。避免使用“预览图”，以免与生成结果预览混淆。
- **无图片模型状态**：没有可用图片模型时，页面仍可浏览提示词、查看本地历史和复用/复制提示词，但生成操作禁用。
- **GPT Image 2 参数**：绘图工作台首版只展示 `gpt-image-2-official` 的核心参数 `size`、`quality`、`n`；`background`、`moderation`、`output_format`、`output_compression`、`watermark` 暂使用后端默认值，不在页面展示。


- **APIMart 渠道**：将本站图片生成请求委派给 APIMart 的渠道；当前支持 `gpt-image-2-official`，未来可扩展其他 APIMart 模型。
- **同步代理**：本站在一个下游 HTTP 请求中等待上游异步图片任务终态，再返回标准图片响应。
- **参考图**：用户为图生图提交的一张或多张源图片。
- **遮罩图**：与参考图配套、限定局部重绘范围的单张图片。
- **预设输出 token**：按画面宽高比、分辨率和质量预估的单张图片输出 token，用于预扣；正常终态以 APIMart 完成任务报告的用量结算，缺失或无效时按预估用量结算且不调整预扣。
- **模型倍率计费**：按请求实际或预估 token 乘模型及用户组倍率计算额度的模式。
- **按次计费**：按生成图片数量乘管理员配置的固定单价计算额度的模式。

## 认证与模型调用

- **用户会话调用**：用户通过已登录的用户会话调用标准同步 `/v1` Relay；其授权主体是用户，而不是某个 API token。会话请求使用不落库的虚拟 Token 上下文，计费来自用户钱包或订阅，真实 API token 额度不变。
- **API token 调用**：调用方通过 API token 发起的模型请求；其模型、分组、额度和网络限制由该 token 的配置决定。
- **会话 Relay 分组**：默认使用用户分组，也可在请求体中选择用户可用分组；日志中的虚拟 Token 名称为 `playground-<实际分组>`，`token_id` 为 `0`。
- **Midjourney 会话调用**：已登录用户使用用户会话调用 canonical Midjourney Imagine 提交、单任务查询和批量查询；请求复用不落库的虚拟 Token 上下文、现有 Midjourney 分发与任务链路，沿用 Midjourney 的钱包计费和失败退款语义，不调整真实 API token 额度。其他 Midjourney 动作和兼容路径仍只接受 API token。
