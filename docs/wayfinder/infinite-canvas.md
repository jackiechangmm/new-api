# 无限画布集成路线图

## 目标与状态

在尽量保留 infinite-canvas 既有图片画布交互与工程结构的前提下，明确接入 new-api 登录认证、模型 Relay、云端工程、精选词库和数字资产所必需的差异，形成可实施的集成方案。

规划已收敛，可进入实施：产品范围、复用原则、认证与模型接入方向及工程云端存取行为均已确认。具体模型名单、接口与存储结构、更新检查方式和协议适配验收在实现阶段完成。本文不表示集成已经实现或通过验收。

## 文档维护

- 本文是本次 Wayfinder 的唯一规划入口，集中保存当前有效的决定、依据和待决问题；不再通过 GitHub Issues 分散维护决策。
- 决策变化时原位更新，历史由 Git 保存；正文不堆积讨论记录、废弃方案或逐轮决议。
- 每轮围绕一个必要接入问题推进。先查源码可确认的事实，再让用户决定产品取舍；尚未确认的方案留在待决问题中。
- 达到可实施程度即结束规划，接口命名、文件拆分及一般构建优化由实现阶段处理。出现影响范围或用户承诺的新事实时再回来决策。

## 已确认方向

### 复用上游交互

- 默认保留上游已有的画布操作、资产选择、资产插入与节点收录体验，只调整本站接入所必需的部分。
- 资产库接入重点是替换本地数据源、映射数据结构和接入用户权限，不重新设计拖拽或双向交互协议。
- 工程持久化沿用上游工程文档结构，按保留的能力保存必要内容，不重新定义一套画布内部模型。
- 功能范围约束用户可见能力；具体代码裁剪必须依据依赖关系，不以文件名或删除清单代替分析。

### 首期产品范围

- 在 new-api 的“AI 应用”中提供画布入口，保留主站导航；画布提供中文界面。
- 聚焦图片画布，不开放视频、音乐及独立 Agent 工作流；不提供图片超分能力。
- 用户无需配置 API Key、Base URL 或外部同步服务，模型请求通过本站 Relay 调用并沿用本站计费。
- 提供经验证的受控图片模型集合；提示词修饰、图片反推等辅助能力使用固定 LLM，具体名单在实现阶段维护。
- 精选词库使用本站数据，个人资产使用本站数字资产，不依赖外部词库作为本站精选内容来源。

### 认证与模型接入

- 主站已登录的用户打开内嵌画布即可使用，无需再次登录或配置。主站管理登录、会话刷新和退出，画布通过小型宿主接口取得有效认证头，直接请求本站 Relay，并保留请求取消及流式结果处理。
- 复用主站已有会话能力，不在画布另建认证体系。会话失效交由主站统一处理；认证凭据仅供运行时请求使用，不写入画布配置、工程或本地持久化。
- 面板 Access Token 通过 `Authorization` 携带，不作为上游通用 API Key 配置；不同模型协议统一接入本站会话认证。具体宿主接口形式在实现阶段确定，按同域受信任子应用设计，不将同域内嵌视为安全隔离。
- 独立维护画布模型与能力配置，定义允许的操作、参数范围和请求协议；不自动继承作图广场全部模型。作图广场已有实践作为候选与复用依据，具体名单和辅助 LLM 选择不阻塞初期规划。
- 能力配置同时驱动界面和发送前校验，结合用户实际可用模型决定可执行范围。打开已有工程也校验其模型和参数，不因旧数据绕过限制；模型不可用时明确提示，不擅自换模型执行。
- 受控名单是画布的产品范围，不是新增的服务端授权规则。本站 Relay 继续执行用户原有权限、分组及计费规则，不改变用户在其他 AI 应用或 API 中的权限。
- 保留上游操作入口、交互流程及可复用的请求构造与解析，在必要位置适配本站认证与协议。实现阶段逐项验证后开放能力，不复制整套作图广场，也不预先建设全站通用 AI 调用框架。

### 工程云端存取

- 长期定位为在线应用，不支持离线使用。云端是工程的唯一正式保存位置，按用户隔离；保留上游创建、打开、导入、重命名、删除和编辑体验。
- 修改后空闲时自动保存，只有服务端确认后才表示保存完成。短暂断网时保留当前页面内容并提示未保存，恢复连接后尝试保存；不承诺关闭页面后恢复未保存修改，离开前给予提醒。
- 用户回到已打开的画布时检查云端更新；没有未保存修改或进行中的工作需要保护时，自动跟进最新工程，不让普通的跨设备使用变成冲突处理。具体检查与协调方式保持简单，在实现阶段确定。
- 本地未保存修改与云端更新冲突时，保留本地内容并停止自动覆盖，允许另存为新工程，或放弃本地修改并打开云端版本；首期不提供强制覆盖或自动合并。
- 云端已删除的工程不因旧页面保存而复活；有本地未保存内容时可另存为新工程，否则提示删除并返回列表。
- 正常的连续编辑、自动保存和图片生成结果不应相互覆盖，也不应制造需要用户处理的冲突。

### 图片上云

- 生成结果、本地导入、粘贴、裁剪和分割产生的图片统一上云。
- 云端图片独立于数字资产。上传不自动收录；“存入资产”仅创建对已有云端图片的资产条目。
- 记录图片身份和存储信息，画布与图片资产通过图片 ID 使用图片；从已有图片资产加入画布时复用对象。
- 首期删除节点、画布工程或图片资产均不删除对应云端文件，不额外维护引用表、引用计数或回收任务。
- 接受云端图片持续占用存储、可能存在遗留对象的代价；未来有实际需要时再设计回收和存量关系重建。
- 上传、保存失败如实提示，不将失败标记为已同步；首期不建设完整的未同步草稿恢复体系。
- 使用公开读 S3，精选词库与云端图片复用通用存储能力。图片身份和写入操作仍须按用户授权；公开读不等于私密文件存储。

### 工程组织与交付

- 画布源码纳入根目录 `canvas/`，与 `web/` 独立依赖、独立构建，同仓提交、统一发布，不使用 Git Submodule。
- 控制台入口为 `/playground/canvas`，以同域子应用形式接入，复用主站认证能力。
- 子应用制品装配到 `web/dist/canvas/`，复用现有 Go 静态托管和发布流程；正确区分页面路由兜底与缺失静态文件的 404。
- 扩展现有 `makefile`、Docker 和开发编排，覆盖资源路径、开发代理及热更新，不另建一套交付流程。
- 体积与缓存按现有构建能力及实际验收结果处理，没有测量依据时不设独立优化规划。

## 分阶段实施与验收

### 拆分结论与行数口径

不建议把“认证与全部模型”“工程上云”“图片上云”各做成一个 800–1200 行的提交。上游的协议分支、工程同步竞态和图片来源数量，决定了这三个主题都需要继续拆分。

推荐顺序为：**应用装配 → 产品范围与宿主认证 → 最小图片生成 → 图片云端身份与全入口覆盖 → 工程云端保存与同步 → 图片编辑和辅助模型 → 精选词库与数字资产。**

先完成图片身份再把工程云端保存开放给用户，避免出现“工程已保存，换设备图片却丢失”的中间产品。工程服务端可以并行准备，但工程上云阶段不能跳过图片依赖验收。图片编辑、辅助 LLM 和资产库不阻塞最小生成与保存闭环，放在后面逐步开放。

行数约束采用以下口径：

- 每个 M 阶段对应一个可独立审阅、可验收的集成 diff；新增行与删除行相加，不看净增长。测试、翻译、文档及手写配置均计入。
- 800–1200 行是实施预算，不是已经证明的工作量；不为凑足 800 行增加抽象或测试。预计超过 1200 行时，先调整交付边界，不能把验收必需的失败路径推给下一阶段。
- 独立列出锁文件、自动生成文件与原样上游源码的行数，不把它们伪装成零成本。禁止通过压缩代码、搬进生成文件或拆成不可运行的小提交满足限额。
- **源码首次入库是无法满足该限制的例外，需实施前确认。** 当前仓库没有 `canvas/` 源码；`infinite-canvas-dist/` 是制品目录，不是源码集成。临时目录中的完整上游应用有数万行，仅画布交互主体也远超 1200 行。建议用独立的 `V0` 原样源码基线提交，记录提交号、来源和许可证，不夹带功能改动；后续 M 阶段才执行集成 diff 预算。V0 不计作一个已交付功能阶段。
- 若首次源码引入也必须严格小于 1200 行，则“保留上游交互、同仓纳入源码、每阶段可运行且人工验收”无法同时成立。不建议把完整应用机械切成十几个不能运行的搬运阶段。
- 大规模删除上游未使用实现同样计入 diff，不承诺塞进某个收尾阶段。首期先封闭路由、能力注册和执行入口；可保留不可达历史代码。确有必要的物理删除另列审阅，不以“清理”为名扩大某个阶段。

### 开发验收规则

- 从 M0 开始统一从主站 `/playground/canvas` 进入。日常人工验收使用 `make dev` 的默认 `http://127.0.0.1:5173/playground/canvas`；正式制品验收使用 `make preview` 的 `http://127.0.0.1:5174/playground/canvas`。
- M0 即接通开发代理、画布热更新及正式构建装配；不能到最后才发现 iframe、资源路径或 Cookie/会话刷新只能在独立开发服务器中工作。
- 未完成的能力不显示为可用，旧工程也不能通过遗留配置触发。M0 至 M4 为集成预览，不对外承诺工程已云端保存；页面明确说明当前保存限制。默认不向普通用户开放半成品入口。
- PostgreSQL、Redis 使用隔离的开发/预览环境。准备两个普通账号 A/B、A 的两个浏览器会话、一个可用图片模型、一个辅助 LLM、公开读 S3 测试前缀及必要 CORS。真实生成会消耗额度，使用测试账号并记录消耗日志。
- 每阶段运行受影响包测试和画布类型检查/构建；涉及主站装配时执行 `make build-web`。数据库迁移、并发写入和所有权校验必须在 PostgreSQL 验证。发布前执行 `make test` 与 `make preview`。
- 人工验收负责真实交互、S3 CORS、会话和协议；自动化测试负责可确定重现的竞态及异常。不能用“手动同时点击”代替保存乱序、旧生成结果晚到等回归测试。
- 验收记录只写实际运行的命令和结果。本节来自静态源码阅读，并不表示以下阶段已构建、已联调或已通过浏览器验收。

### 阶段总览

下表预算包含实现、测试和必要说明；V0 除外。具体模型和协议名单在对应阶段开始前根据本站可用上游确定，不默认扩大到全部 OpenAI/Gemini 能力。

| 阶段 | 可交付结果 | 依赖 | 集成 diff 预算 |
| --- | --- | --- | --- |
| V0 | 固定并原样纳入上游源码基线，保留来源和许可证 | 确认首次引入例外 | 数万行，单独审阅 |
| M0 | ✅ 已完成：主站入口、子应用构建与开发预览 | V0 | 800–1000 行 |
| M1 | 已完成并通过人工验收：图片范围封闭、中文界面与宿主认证 | M0 | 900–1200 行 |
| M2 | 已实现并通过真实出图、会话消费归属验收 | M1 | 已确认本阶段可超过 1200 行 |
| M3 | 云端图片身份及本地导入闭环 | M1 | 900–1200 行 |
| M4 | ✅ 已完成：生成、粘贴、裁剪、分割统一上云 | M2、M3 | 900–1200 行 |
| M5 | 云端工程 CRUD、导入和明确保存 | M4 | 1000–1200 行 |
| M6 | 连续编辑下可靠的自动保存 | M5 | 800–1100 行 |
| M7 | 跨设备更新、冲突处置和云端删除 | M6 | 900–1200 行 |
| M8 | 参考图编辑协议 | M4；完整体验依赖 M7 | 800–1100 行 |
| M9 | 固定 LLM 的提示词修饰与图片反推 | M2、M4 | 800–1100 行 |
| M10 | 本站精选词库 | M1 | 800–1000 行 |
| M11 | 本站个人文本资产 | M1；完整体验依赖 M7 | 800–1100 行 |
| M12 | 图片数字资产双向复用 | M4、M11 | 900–1200 行 |

M3 可与 M2 并行准备，M10 可独立提前；推荐按表中顺序串行验收，优先完成 M0–M7 的“生成图片并跨设备继续编辑”闭环。M8 只承诺一个已验证的编辑协议；若受控名单要求额外的 Gemini 原生或其他图片协议，每个协议增加一个同口径阶段，不偷偷并入 M2/M8。

### M0：应用接入与统一交付

**范围**：独立 `canvas/` 包及资源基路径、主站认证路由内的承载页面、AI 应用入口、Go 静态托管、makefile、Docker、开发及预览编排。优先保留上游源码布局，避免无关文件移动和格式化。

**边界**：本阶段只开放画布基本操作用于预览，模型、外部服务配置及不在首期范围内的页面不得因原样引入而对外开放。M1 完成运行时能力封闭；这里先保证可见和直达路由入口均受限。不在 M0 同时实现认证刷新桥、模型适配和工程同步。

**人工验收**：

1. 已登录用户从主站进入，主站导航保留；未登录访问仍由主站处理。
2. 新建预览工程，添加文本/图片节点、拖动、缩放和连线可用；不显示“已云端保存”。
3. 修改画布源码可热更新，主站其他 AI 应用不受影响。
4. `make preview` 中刷新主站画布入口、打开工程内部页面均正常。
5. `/playground/canvas` 返回主站承载页面；子应用页面由 `/canvas/` 下约定的页面路由加载。缺失的 `/canvas/assets/not-found.js` 返回 404，而不是任一应用的 HTML。不要把主站画布路由直接替换成子应用 HTML 导致导航消失。

**自动验收**：页面兜底与缺失静态资源的 HTTP 契约；独立构建及统一制品中画布资源存在。

**M0 实施记录**：对应提交 `46930a10`。已完成 `canvas/` 子应用纳入同仓、`/playground/canvas` 主站认证路由入口、`/canvas/` 子应用资源基路径、Go 静态托管与页面兜底、主站与画布独立开发服务器及代理、`make build-web`/Docker 统一构建装配。画布开发服务器使用 3001 端口，主站开发服务器使用 5173 端口；正式制品装配到 `web/dist/canvas/`。后续 M1 修复了画布路由运行时导入遗漏，但不改变 M0 的交付边界。

### M1：宿主认证与首期能力边界

> 本阶段“关闭”统一表示用户触达不到：关闭相关入口、路由、能力注册和执行路径；核心代码保留，不为此进行大规模物理删除。项目安全性要求较低，用户切换以可见数据隔离为准，不额外引入清除缓存、复杂加密或完整本地数据生命周期管理，以节省流量并保持实现简单。

**范围**：小型宿主接口获取有效认证头，刷新/失效/退出交由主站；固定中文；限制为图片画布，关闭视频、音乐、独立 Agent、超分、用户 API Key/Base URL、WebDAV、代理和任意模型插件入口。关闭导入和导出能力。

**关键边界**：不仅隐藏菜单，还限制路由、导入的节点类型、能力注册和发送入口；“关闭”以用户不可触达为准，核心代码可以保留，不大批删除历史代码。导入和导出入口及相关路由均不可触达。仅接入认证消费接口，不另造一套 Token 存储/刷新状态机。不同账号切换须隔离用户态可见数据，不能读出上一用户的本地工程/资产缓存；在安全性要求较低的前提下，不要求清除本地数据，以避免额外流量和复杂度。

**人工验收**：

1. 打开画布不二次登录、不要求配置；可通过现有用户模型接口验证会话已接通。
2. Access Token 过期后请求由主站刷新恢复；明确退出后画布不能继续请求。
3. A 退出、B 登录，画布不残留 A 的可见内容；本地数据可以保留，但必须按用户态隔离，不能因切换直接展示 A 的工程或资产。
4. 检查画布配置、IndexedDB 和工程导出：没有宿主认证凭据。主站已有认证存储不在此重构。
5. 直达隐藏页面、导入含视频/Agent/外部配置的工程、导入导出入口，均不能执行或触达受限能力；已有图片交互保持可用。

**自动验收**：认证刷新/退出契约、凭据不进入子应用持久化、导入与程序调用的能力限制。具体请求中断由 M2 验证。

**M1 实施记录**：初始提交 `71b80ed9`、`af3c552c` 完成了部分路由和入口关闭，但认证桥未被消费、用户身份未初始化，不能视为完整通过 M1 验收。本次补齐以下行为，自动验证通过后已由用户确认人工验收通过：

- 宿主提供绑定当前用户与登录会话的身份、认证头和失效订阅；认证头复用主站 `getFreshAuthHeaders`，刷新期间退出或换账号时拒绝晚到凭据。桥安装完成后才挂载 iframe，用户或会话变化时重新挂载；不向持久化配置写入凭据。
- 画布先确认身份并通过 `/api/user/models` 验证认证链路，再恢复该账号的工程与资产。恢复失败不进入画布，可重试；直接打开子应用不能加载工程。旧匿名缓存保留，不自动归给当前账号。
- 延迟工程保存绑定提交时的账号；图片清理只处理当前账号的文件，不清理其他账号或历史匿名文件。退出只关闭可见运行时，不主动清空持久化缓存。
- 固定中文；关闭视频、音频、插件、Agent、超分、模型执行、外部配置、外部词库同步以及工程导入导出的入口和相关执行路径。旧工程包含不支持的节点或生成模式时拒绝打开，原数据保留。图片导入、文本编辑、节点移动、连线、分组和本地资产交互仍保留。
- 明确提示工程仅保存在当前浏览器、尚未上云。生成与模型化参数设置仍属于 M2，本轮不开放。

**自动验证**：`cd canvas/web && bun run test`；`cd canvas/web && bun run test:e2e`（依赖正在运行的主站开发环境，默认 `http://127.0.0.1:5173`）；画布和主站 `bun run typecheck`；主站宿主认证与现有会话测试；受影响文件 lint；`make build-web`。浏览器测试使用模拟认证响应，不消耗模型额度；覆盖真实主站 iframe 接入、过期前刷新、A/B 工程和资产隔离、入口封闭、退出与独立访问限制。真实账号、真实刷新接口及实际图片编辑交互仍由人工验收确认，不能以模拟响应代替。

**人工验收入口**：`http://127.0.0.1:5173/playground/canvas`。分别验证账号 A 新建工程、编辑文本、导入图片、裁剪与分割、本地资产收录；退出后 B 不可见 A 的数据，切回 A 仍可恢复；检查受限入口不再出现，并确认刷新/退出交由主站处理。用户已确认本轮人工验收无问题；M2 尚未实施。

### M2：受控图片生成最小闭环

**范围**：先选一个真实可用模型及其生成协议；能力配置驱动 UI 与发送前校验；复用上游请求构造和解析，通过宿主认证直接请求本站 Relay；处理取消、错误、单图/多图结果。尚未上云时结果仅作为预览，不宣称正式保存。

**交付预算例外**：用户已确认完整 M2 可以超过 1200 行，不拆为 M2a/M2b。仅本阶段适用，不扩大到图片编辑、云端保存、移动端适配或全站模型框架。

**关键边界**：本站已有 `/api/user/models` 可用于用户分组下的可用模型交集，不调用仍要求 API token 的 `/v1/models`，不新增服务端画布模型授权体系。受控名单只限制画布产品能力；Relay 原有权限、数量上限及计费规则继续生效。没有协议联调依据时不修改 Relay 公共实现。

**已确认的参数行为**：

- 按模型与操作维护受控能力配置，共同驱动 UI、发送前校验和协议编码；不支持的参数不显示、不发送，不使用全模型通用参数表。
- 分辨率、宽高比与画质分别保存。image2 不提供任意像素宽高输入，发送语义尺寸，具体格式以本站协议验证为准，不预设为 `2k:16:9`。其他模型按能力使用分辨率与比例、固定尺寸预设或自定义像素输入。
- 用户主动换模型时保留兼容值，不兼容值采用目标模型默认值并提示调整项；打开旧工程保留原值，阻止不兼容配置执行，不静默换模型、不取最近比例。本期不增加按模型记忆设置。
- 数量受单次请求能力限制，不自动拆请求或补足返回数量；设置面板、配置节点、生成和重试共用有效参数解析。
- M2 仍只验收一个真实模型协议，不借此建设全站能力平台。

**模型能力与参数行为（已确认）**：

- 画布独立维护按“模型 + 操作”组织的受控能力配置，统一驱动图像设置、发送前校验和请求编码。不自动继承作图广场模型名单，不建设全站通用模型能力平台。
- 分辨率、宽高比、画质分别保存，避免先转成像素再反推用户选择。尺寸交互服从模型能力：语义尺寸模型提供分辨率和比例，固定尺寸模型提供合法预设，只有确实支持自定义像素的模型才提供宽高输入。
- `gpt-image-2` 不展示任意像素尺寸输入及透明背景选项；比例、分辨率、画质和数量使用经验证的模型配置。发送时保留分辨率与比例语义；当前作图广场使用 `size: "16:9 2k"`，画布具体编码须按本站真实协议验证，不将 `"2k:16:9"` 预设为协议契约。
- 不支持的参数不展示、不发送；不可因隐藏控件而继续从全局配置或旧节点继承发送。能力配置须能表达合法组合，而不仅是独立选项列表。
- 用户主动切换模型时保留兼容值，不兼容值改为目标模型默认值并明确提示调整项；不推测最近比例，不增加各模型独立设置记忆。
- 打开旧工程时保留原模型及参数；失效模型或不兼容参数阻止执行，要求用户明确修改，不静默换模型、修正参数或近似比例。
- 生成数量受单次协议能力限制，保留单次请求返回多图；不自动拆成多个请求、不自动补足上游返回不足的张数。跨请求批量调度、部分失败补偿与补图计费不纳入 M2。
- 设置面板、配置节点、生成和重试共用有效参数解析规则，避免各入口独立拼接节点参数与全局默认值。
- M2 仍只联调并开放一个真实模型协议；以其他已有模型的差异检查配置设计，不为验证抽象而提前开放额外协议。

**人工验收**：

1. 无 Key 配置完成真实生成；后台消耗日志归属当前用户，真实 API token 额度不被会话请求扣减。
2. 不可用模型不执行，非法数量/尺寸在发送前阻止；旧工程不静默换模型。
3. 取消后晚到结果不回写；配额不足、上游失败有清晰错误。
4. 生成期间移动节点、修改标题，结果只更新目标节点，不恢复旧工程快照。

**自动验收**：模型交集、按模型及操作的参数边界、主动换模型的参数调整、旧工程无静默修正、不支持字段不发送、语义尺寸编码、单次请求数量限制、响应格式、AbortSignal、节点删除及请求替换后的晚到结果。

**M2 实施记录**：

- 接入受控 `gpt-image-2` 文生图，按模型与操作维护设置、默认值和校验；分开保存分辨率与比例，本站 Relay 请求使用 `size: "比例 分辨率"`。旧工程不自动修正，主动选择模型才调整不兼容值并提示。
- 宿主认证头仅在请求时获取；模型列表与 `/api/user/models` 取交集，发送前再次验证权限。生成固定请求本站 `/v1/images/generations`，不读取旧渠道密钥、脚本或外部地址。
- 一次请求发送 `n` 并保留返回图片；不足时提示，不拆请求或自动补图。生成与重试使用同一执行流程，保护节点修改、请求替换、取消、节点删除和离开工程后的迟到结果。编辑、辅助模型与外部能力继续封闭。
- 已运行并通过：画布 `bun run typecheck`、`bun run test`（12 项）、`bun run test:e2e`（6 项，包含原 M1 回归）、根目录 `make test`、`make build-web`。画布构建仍有既有的大包和静态/动态导入混用告警；本阶段不做专项打包重构。未执行正式隔离环境 `make preview`。
- 在开发环境创建普通测试账号 `cm2_1788932958634`（用户 ID 3），仅为该账号初始化 500000 测试额度，通过主站登录和画布界面发起一次 `gpt-image-2`、`1:1 1k`、`low`、`n=1` 的真实请求。上游返回 HTTP 402：余额 0.044362 USD，所需 0.050000 USD。失败后测试账号额度恢复为 500000、已用额度为 0，未产生消费日志。
- 渠道充值后再次通过主站登录和画布界面真实请求：普通测试账号 `cm2_1788934023469`（用户 ID 4），`gpt-image-2`、`1:1 1k`、`low`、`n=1` 成功出图并显示。消费日志归属用户 4、渠道 1，`token_id=0`、消费 2390；用户额度从 500000 降至 497610，无浏览器运行错误。截图 `/tmp/new-api-canvas-m2-real.png`。随后以普通账号 `cm2_1788934642905`（用户 ID 6）创建一个 100000 配额的真实 API token，再经画布执行同样请求：成功展示图片，消费 2390、`token_id=0`，用户余额 497610；已有 token 的剩余额度与已用额度前后完全一致，补齐真实 token 额度不变验收。
- 双轴审查已复核：旧模型不自动规范化符合明确保留旧值的设计；认证失效后的重试改为整页重新初始化，避免复用旧模型列表；遗留数量缺省值统一为 1。现阶段 image2 使用已接入的独立参数组合，尚未增加其他模型的组合限制或像素输入界面。
- 侧边栏全链路集成补齐：在主站“AI 应用”导航中接入“无限画布”（`/playground/canvas`）一级入口，使用 `InfinityIcon`；将 `canvas` 模块规范纳入管理员及用户侧边栏配置体系（`chat.canvas` 默认启用），对齐 `isSidebarModuleEnabled('chat', 'canvas')` 路由拦截；补齐 7 国语言包。
- 图像设置原生 UI 体系与文案规范已形成共识并立项 Issue #20 待修复。

### M3：通用 S3 能力、云端图片身份与基础上传闭环

**范围**：
- 抽取系统级通用 S3 存储驱动（统一规范 `S3_*` 配置），重构精选词库底层复用该通用驱动，保持外部行为与管理接口完全无回归。
- 建立全站通用的 `images` 实体模型与服务端代理上传接口（`POST /api/images`），限制 10MB、支持常见格式（JPEG/PNG/WebP），在服务端强制通过标准库解码校验真实图片魔数并提取精准宽高，落库并返回全局唯一的图片 UUID 与公开 URL。
- 画布前端接入宿主认证桥，在 `image-storage.ts` 中将底层 `uploadImage` 替换为云端上传，节点元数据复用 `storageKey` 字段保存云端图片 UUID，打通本地文件导入、剪贴板粘贴与裁剪分割生成的 Blob 上云闭环。

**关键边界**：
- 前端数据模型复用现有的 `metadata.storageKey` 承载云端图片 UUID，保持上游流转逻辑零破坏；`content` 存储 S3 公开读 URL。
- S3 存储桶配置公开读与 CORS（`Access-Control-Allow-Origin: *`），前端加载公共图片统一支持跨域，避免 Canvas 像素读取污染导致裁剪/分割报错。
- 权限极简：上传接口返回完整元数据；元信息查询接口保持轻量，不人为增加复杂的跨用户私有阻断。
- 首期不引入对象回收计数、物理删除或通用媒体平台，精选词库的 S3 删除能力仅供其自身封面替换使用，画布图片写入后不提供物理删除接口。

**人工验收**：
1. 本地选择文件导入、剪贴板粘贴图片均上传成功并获得云端 UUID（`storageKey`），在开发者工具中可确认调用 `/api/images` 与返回元数据。
2. 刷新页面或在新窗口打开节点图片，可通过 S3 公开 URL 正常显示；对上传的 S3 图片执行裁剪或分割，验证 Canvas 像素读取正常，无跨域污染报错。
3. 上传非图片伪装文件、损坏文件、超 10MB 文件或 S3 离线时，接口明确返回失败，画布正确提示错误且不产生假身份。
4. 精选词库原有的封面上传、展示、替换删除行为完全无回归。

**自动验收**：S3 驱动抽象与环境变量解析、通用图片表 CRUD 与真实图片二进制校验、画布 `uploadImage` 云端适配与状态映射、精选词库回归测试。

### M4：生成图转存上云与全来源收口

**范围**：
- 聚焦 AI 模型生成结果（单图、多图批量出图、重试生成）统一由前端/服务端安全转存至云端 `images`，彻底替换上游对 `localforage` 的本地持久化依赖。
- 覆盖助手出图、参考图中间结果等遗留调用路径；清理上游原有的 `cleanupUnusedImages` 逻辑，确保删除节点或关闭页面绝不误删云端 S3 对象。
- 协调多图并发上传、取消请求、切换工程及晚到生成结果的归属保护；为 M5 工程云端保存提供“图片全部确认上云”的前置保障。

**关键边界**：
- 移除上游“远程下载失败后把原 URL 当上传成功”的回退语义。释放浏览器临时 Blob URL 与操作云端对象必须严格解耦。
- 上传失败保留当前页面可见内容并允许重试，绝不触发重新计费生成。
- M5 保存工程前必须确认所需图片已完成上传，未上云的图片不得写入云端工程文档。

**人工验收**：
1. 真实触发 AI 生成出图，单图与多图结果均自动转存云端并取得稳定 UUID。
2. 模拟网络异常中断上传，当前生成预览保留且提示未同步，恢复后可手动重试上传，无需重新消耗额度生成。
3. 在生成转存期间快速删除节点、切换工程或重试，迟到的结果不会错乱插入当前画布。
4. 彻底断开 IndexedDB/localforage 依赖，验证画布图片不再写入本地 IndexedDB，删除节点亦不删除云端对象。

**自动验收**：生成图转存流转、取消与晚到保护、批量上传竞态、不调用 S3 物理删除。

### M5：云端工程 CRUD 与明确保存

**范围**：按用户隔离的工程模型/API、列表、新建、打开、重命名、删除、导入及明确的保存操作；沿用上游工程文档结构；去掉正式工程的 localForage 持久化。提供服务端 revision 条件更新，修改与重命名不能各自无条件覆盖文档。

**关键边界**：这是可人工验收的中间保存方式，下一阶段改为空闲自动保存，不代表改变最终产品决定。此阶段就必须安全处理冲突：保留本地内容、停止覆盖，并能另存或重新打开云端。更新不存在的 ID 不得变成创建；不要求为此单独建设 tombstone 系统。校验工程大小、结构、图片归属及保留节点能力；导入含本地图片的文档须完成图片归一化，无法恢复的本地引用明确报错，不假装导入完整成功。

**人工验收**：

1. A 新建并保存含图片的工程，在另一浏览器打开，节点、连线、视口与图片一致。
2. B 列表不可见，猜测 ID 也不能读取、修改或删除 A 的工程。
3. 重命名、删除、导入、刷新均符合界面结果；离开未保存页面有提醒。
4. 保存失败保留内容；保存响应之前不能显示已保存。
5. 两页基于同一 revision 保存，仅一个成功，另一页可另存或打开云端；删除后旧页保存不能复活。

**自动验收**：目标 PostgreSQL 上的 CRUD、所有权、条件更新并发、删除后写入、图片引用及导入文档边界。

**M5 实施记录**：对应 Issue #23。
- **服务端云端工程实体与租户隔离**：新增 `model.CanvasProject`（`canvas_projects` 表），包含 `id`, `user_id`, `title`, `revision`, `content`, `created_at`, `updated_at`。挂载 `/api/canvas/projects` RESTful 路由并接入 `middleware.UserAuth()`，所有写查严格绑定当前登录用户 `user_id`。
- **CAS 条件更新乐观锁**：PUT 接口执行 `UPDATE canvas_projects SET ..., revision = revision + 1 WHERE id = ? AND user_id = ? AND revision = ?`。版本不匹配返回 `409 Conflict` 并带回服务端最新 revision；工程不存在或已删除返回 `404 Not Found`。
- **包体上限与极简校验**：单次工程包体在控制器层限制不超过 5MB（超限返回 413），防范异常大包。
- **前端纯内存运行时**：彻底剥离 `useCanvasStore` 的 `zustand/persist` 与 `localForage` 存储，正式工程由服务端 API 动态加载与持久化，关闭工程导入导出功能。
- **10 秒无感自动保存 + 显式保存排队状态机**：实质内容变动标记 `isDirty = true` 并启动 10s 空闲倒计时；纯视口平移缩放不计入 dirty；提供顶部保存按钮及 `Ctrl/Cmd + S` 立即保存；传输中（In-flight）的新编辑受到排队保护不误标为已保存；`beforeunload` 安全拦截未保存离开。
- **409 冲突模态阻断**：遇到版本冲突立即停止覆盖并弹窗阻断，支持“另存为新工程”或“放弃本地修改重新拉取”。
- **自动化测试**：后端 HTTP 契约测试 `controller/canvas_project_test.go`（覆盖 CRUD、CAS 乐观锁、租户隔离、删除防复活及大包体限制）；前端端到端测试 `canvas/web/tests/m5.spec.ts`（覆盖即时创建与导航、未保存与显式/快捷键保存、纯视口不致脏、传输中编辑保护、409 模态处置及 IndexedDB canvas_store 彻底移除校验）。全部自动化测试通过。

### M6：可靠自动保存

**范围**：空闲自动保存、当前编辑版本与已确认版本、单工程串行提交、保存中继续编辑的后续保存、失败提示与恢复重试、离开提醒。复用 M5 冲突底线，不重做工程 API。

**关键边界**：旧保存成功只能确认它提交的版本，不能把随后修改标为干净；自动保存响应不能整份覆盖当前画布。生成和上传完成是正常编辑，使用同一保存通道。未上传图片不进入“已保存”文档。

**人工验收**：

1. 连续编辑后停顿，自动出现保存中、已保存；刷新得到最后内容。
2. 慢网络下保存期间继续输入、移动和生成，最终云端包含所有有效修改，无正常操作导致的冲突弹窗。
3. 断网后保留画布并显示未保存，恢复后保存；断网离开有提醒。
4. 编辑中退出账号或换工程，旧请求不能把新工程/新账号界面标为已保存。

**自动验收**：使用可控制完成时机的请求测试，验证保存排队、旧确认、失败重试、上传与保存协调；不用 sleep 猜竞态。

### M7：跨设备更新与冲突完整体验

**范围**：返回页面时检查云端 revision；没有未保存修改或进行中工作时自动更新；存在本地修改时停止覆盖，另存为新工程或放弃本地打开云端；补齐云端删除时的分支。

**关键边界**：正在生成/上传也属于需要保护的工作，即使文档暂时没有 dirty 标记也不能直接替换。版本检查与自动保存需要协调，不能把本页刚保存的版本误判成其他设备冲突。不增加协作订阅、自动合并或强制覆盖。

**人工验收**：

1. A 的两页打开同一工程，第一页面保存；第二页无修改，切回后自动看到更新。
2. 第二页有修改或生成/上传尚未完成，云端更新不能覆盖当前内容。
3. 发生冲突后分别验证另存和放弃本地；普通重试不应越过冲突继续写。
4. 云端删除：无本地工作返回列表；有未保存内容允许另存，原 ID 始终不会复活。

**自动验收**：返回检查与保存交错、生成保护、删除与保存交错、另存不改原工程、放弃本地后的晚到任务隔离。

### M8：参考图编辑

**范围**：选定一个实际需要的图片编辑协议，覆盖云端图片转请求资源、参数校验、编辑结果上传和工程保存。按上游交互保留参考图选择及顺序；遮罩只有模型与上游操作均支持时才开放。

**人工验收**：从已云端保存的图片添加参考图并编辑；换浏览器重复执行；输出生成新的图片身份而不修改原对象；不支持的参考图数量/参数阻止提交；取消和失败保留原图。

**自动验收**：请求协议及资源顺序、认证头不进入上游配置、引用图读取错误、结果接入 M4/M6 的一致性。

### M9：提示词修饰与图片反推

**范围**：固定辅助 LLM 的文本及视觉调用，保留流式显示和取消；复用 M1 认证，不开放任意模型/地址配置，不引入独立 Agent 工作流。

**人工验收**：修饰文本和反推一张云端图片；流式结果正常更新；取消不继续改写；无模型权限/额度不足有明确提示；生成期间切换节点不会写错目标。

**自动验收**：固定模型选择、流式分片解析、视觉图片输入、取消和迟到响应。辅助结果插入文本节点后可正常云端保存。

### M10：本站精选词库

**范围**：保留上游选择、预览与插入交互，替换为本站精选词库数据；正确映射分页、分类/标签、搜索和封面。停用外部精选源与后台同步，不扩建词库管理系统。

**人工验收**：管理员在本站新增/修改词条，普通用户在画布查找并插入；跨页搜索不漏项；本站接口失败时展示错误，不回退外部源；画布插入文本后不影响其他未保存修改。

**自动验收**：真实 API 契约映射、分页/筛选、空数据/错误及插入结果。若仅需很小适配，接受不足 800 行，不通过重写已有管理页面扩大 diff。

### M11：个人文本数字资产

**范围**：替换上游本地资产数据源，先接本站已有文本资产；保存节点为文本资产、查询选择及插入，映射现有编辑、标签、收藏和分页交互。图片资产入口保持不可用，直到 M12。

**人工验收**：文本节点存入资产，在主站资产页修改后重新插回；搜索、分页、标签和收藏与本站一致；A/B 数据隔离；接口失败不产生已保存的假资产。

**自动验收**：资产字段与分页契约、保存失败状态、用户切换缓存隔离。不要把服务端分页后的单页做本地筛选伪装成全库查询。

### M12：图片数字资产

**范围**：扩展现有仅支持文本的数字资产模型/DTO/主站显示及画布映射；图片资产关联已有云端图片 ID，保留上游存入资产、选择和插入体验。

**关键边界**：上传不自动创建资产；存入资产不再次上传；从资产插入画布不复制对象。图片身份及写入按用户校验。既有文本资产数据和接口继续可用。

**人工验收**：

1. 生成图片存入资产，Network 中没有第二次图片上传；在主站资产页能识别和管理该图片资产。
2. 从资产插入另一个工程，复用原图片 ID，跨浏览器打开正常。
3. 删除资产、节点或任一工程，其他引用和公开对象仍可用。
4. 未上传完成的图片不能假装收录成功；B 不能通过 A 的图片 ID 创建自己的图片资产。
5. 原文本资产的创建、编辑、收藏、标签与筛选不回归。

**自动验收**：PostgreSQL 图片归属和兼容性、资产删除不删除文件、引用身份复用、文本资产回归。

### 收口条件

不另设一个包揽“裁剪、可靠性、中文化、发布”的巨大收尾 diff。这些要求必须随所属阶段交付。M12 后运行一次 M0–M12 的联合验收即可；发现问题回到对应边界修复，修复同样遵守行数预算。

最终人工主线为：主站登录 → 创建工程 → 选择本站词条/资产 → 图片生成或编辑 → 裁剪/分割 → 自动保存 → 另一浏览器继续编辑 → 冲突另存 → 图片收录与复用 → 删除工程不删除图片；同时确认不存在用户 Key 配置、外部同步、视频/音乐/Agent/超分入口。

## 待决问题

产品范围没有新增阻塞取舍；实施组织上需确认 **V0 原样源码首次入库不适用 800–1200 行限制**。上表是静态阅读后的预算，若实际改动超限，应在实施前进一步拆出可验收闭环，而非删减必要保障。实施中若发现影响既定行为、范围或长期承诺的新事实，再回到本文决策；不为普通技术细节增设规划议题。

## 不纳入本期

- 重新设计上游已有的画布、资产选择和插入交互。
- 多人实时协同编辑。
- 图片引用维护、自动回收、回收宽限期与续期机制。
- 完整的未同步草稿恢复体系。
- 私有签名 URL 及其刷新机制。
- 缺少实际问题或验收目标的专项性能优化。

## 源码依据

审视基线为 [infinite-canvas](https://github.com/jackiechangmm/infinite-canvas) 的 `d213a74`（v0.18.0），通过浅克隆进行静态阅读，尚未运行交互或构建验证。阶段拆分时核对了 `/tmp/infinite-canvas-review-atC5qp`（`d213a74`）、`/tmp/infinite-canvas`（`ccf5477`）及 `/home/jackie/infinite-canvas`（`9414048`）。这些目录不是同一版本，不混用其文件作为实现基线；建议沿用本文已审视的 `d213a74` 固定首次引入，升级另行评估。

拆分依据包括：

- 基线 `project.tsx` 约 3384 行，`services/api/image.ts` 921 行；不能把全部协议改造和画布裁剪当成一个小适配。
- `use-canvas-store.ts` 146 行，但它是整个工程集合的 Zustand/localForage 延迟持久化，不是带服务端确认和并发控制的云端保存；改造规模不能只按这个文件行数估计。
- `image-storage.ts` 203 行，以本地 Blob、`storageKey`、远程 URL 回退和未引用图片清理为核心。云端 ID、失败语义和不删除文件均需显式替换。
- 本站 `service/featured_prompt_storage.go` 已有 S3 上传/删除，但封面目录、格式及错误语义为词库专用；复用时需保护现有调用，不视为已经具备通用图片身份服务。
- 本站 `model/digital_asset.go`、`controller/digital_asset.go` 当前仅支持文本资产，图片数字资产不是仅替换前端数据源。
- 本站 `web/src/features/drawing/api.ts` 已通过 `/api/user/models` 获取分组下可用模型；`router/relay-router.go` 已支持标准图片/文本与 Gemini 同步 Relay 的会话认证，而模型发现仍保留 API token 边界。
- `router/web-router.go` 的默认兜底返回主站 HTML；子应用静态资源与页面路由必须独立验收，不能仅把文件复制进 `web/dist/canvas/`。

| 接入区域 | 上游阅读入口（相对其仓库根目录） | 本站阅读入口 |
| --- | --- | --- |
| 资产选择与插入 | `web/src/components/canvas/asset-picker-modal.tsx`、`web/src/pages/canvas/project.tsx` | `controller/digital_asset.go`、`model/digital_asset.go` |
| 资产数据与图片存储 | `web/src/stores/use-asset-store.ts`、`web/src/services/image-storage.ts` | `service/featured_prompt_storage.go`、`controller/featured_prompt.go` |
| 工程持久化 | `web/src/stores/canvas/use-canvas-store.ts` | `model/main.go` |
| 模型协议与认证 | `web/src/services/api/image.ts` | `middleware/auth.go`、`web/src/lib/http-client.ts`、`web/src/lib/auth-session.ts` |

图片裁剪、分割等操作需要读取像素；公开对象 URL 仍需满足浏览器跨域读取要求。资产字段、分页和标签的数据映射属于接入实现，不单独扩大成产品设计议题。
